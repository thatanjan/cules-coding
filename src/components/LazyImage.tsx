import React from 'react'
import {
	LazyLoadImage,
	LazyLoadImageProps,
} from 'react-lazy-load-image-component'

const LazyImage = (props: LazyLoadImageProps) => <LazyLoadImage {...props} />

export default LazyImage
